diff -rupN a/cbits/svm.cpp b/cbits/svm.cpp
--- a/cbits/svm.cpp	2011-07-18 08:03:14.000000000 +0000
+++ b/cbits/svm.cpp	2017-06-11 04:57:05.191337403 +0000
@@ -1267,6 +1267,7 @@ public:
 		int start, j;
 		if((start = cache->get_data(i,&data,len)) < len)
 		{
+#pragma omp parallel for private(j) schedule(guided)                    
 			for(j=start;j<len;j++)
 				data[j] = (Qfloat)(y[i]*y[j]*(this->*kernel_function)(i,j));
 		}
@@ -2446,6 +2447,7 @@ double svm_predict_values(const svm_mode
 	{
 		double *sv_coef = model->sv_coef[0];
 		double sum = 0;
+#pragma omp parallel for private(i) reduction(+:sum) schedule(guided)                
 		for(int i=0;i<model->l;i++)
 			sum += sv_coef[i] * Kernel::k_function(x,model->SV[i],model->param);
 		sum -= model->rho[0];
@@ -2463,6 +2465,7 @@ double svm_predict_values(const svm_mode
 		int l = model->l;
 		
 		double *kvalue = Malloc(double,l);
+#pragma omp parallel for private(i) schedule(guided)                
 		for(i=0;i<l;i++)
 			kvalue[i] = Kernel::k_function(x,model->SV[i],model->param);
 
